/// <reference types="cheerio" />
import { Source } from './Source';
import { Manga } from '../models/Manga';
import { Chapter } from '../models/Chapter';
import { SearchRequest } from '../models/SearchRequest';
import { Request } from '../models/RequestObject';
import { ChapterDetails } from '../models/ChapterDetails';
import { LanguageCode } from '../models/Languages';
import { PagedResults } from '../models/PagedResults';
export declare abstract class Madara extends Source {
    constructor(cheerio: CheerioAPI);
    abstract get MadaraDomain(): string;
    abstract get langFlag(): string;
    abstract get langCode(): LanguageCode;
    get titleSelector(): string;
    get authorSelector(): string;
    get genresSelector(): string;
    get artistSelector(): string;
    get ratingSelector(): string;
    get thumbnailSelector(): string;
    get thumbnailAttr(): string;
    get chapterListSelector(): string;
    get pageListSelector(): string;
    get pageImageAttr(): string;
    get searchMangaSelector(): string;
    get searchCoverAttr(): string;
    getMangaDetailsRequest(ids: string[]): Request[];
    getMangaDetails(data: any, metadata: any): Manga[];
    getChaptersRequest(mangaId: string): Request;
    getChapters(data: any, metadata: any): Chapter[];
    parseChapterDate(date: string): Date;
    getChapterDetailsRequest(mangaId: string, chId: string): Request;
    getChapterDetails(data: any, metadata: any): ChapterDetails;
    constructSearchRequest(query: SearchRequest, page: Number): Request | undefined;
    searchRequest(query: SearchRequest): Request | null;
    search(data: any, metadata: any): PagedResults | null;
}
